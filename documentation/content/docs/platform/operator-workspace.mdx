---
title: Operator workspace
description: Wallet login, account-scoped protocol watchlists, and SDK tokens.
icon: WalletCards
---

The current dashboard is not a global public catalog. It is a private
account-based workspace accessed through wallet signatures.

## Login

Login uses a wallet-signed challenge:

1. The client requests a challenge from the backend.
2. The wallet signs the message.
3. The client sends the signature back.
4. The backend creates a session.

## Protocol watchlist entry

A protocol entry links a `programId` to the signed-in STPS account. It is a
workspace watchlist relationship, not a proof that the wallet owns or controls
the protocol.

Core fields:

- `protocolAddress`
- `label`
- `claimedByWallet`
- `status`

## Access scope

The dashboard and SDK return only protocols attached to the authenticated
account. Adding a protocol to the workspace is enough to monitor its score,
flags, certificate, and history.

## SDK tokens

Tokens are persistent and tied to the account. They are not session tokens.

Properties:

- they do not expire automatically;
- they can be revoked;
- they are stored in the database only as hashes;
- the raw value is shown only at creation time.
